{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/Prometheus.MetricsExample.MyConsumer/Prometheus.MetricsExample.MyConsumer.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}